# PHP 8.1 openapi executable from zircote/swagger-php

```
docker run -v "$PWD":/app -it igorslipchenko/php-openapi --help
```

For

```
docker run -v `pwd`:/app -it igorslipchenko/php-openapi -b vendor/autoload.php app/src/
```